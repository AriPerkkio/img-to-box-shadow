export type Options = {
    fileName: string;
    width: number;
    ratio: number;
    useCssVariables: boolean;
}

export type BoxShadow = {
    x: number,
    y: number,
    rgba: string,
}

export type RawPng = [number, number, number, number];